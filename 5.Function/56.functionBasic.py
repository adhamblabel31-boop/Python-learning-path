# ----------------------------------------------------
#! ------------- function and return -----------------
# ----------------------------------------------------
# ? [1] is a reusable block of code do a task
# ? [2] run when you call it
# ? [3] accept element to deal with called (parameters)
# ? [4] can do the task without returning data
# ? [5] can return data after job is finished
# ? [6] create to prevent DRY (don't repeat your self)
# ? [7] accept elements when you call it called (arguments)
# ? [8] there's a built in function and user defined function
# ? [9] is for al team and all apps
# todo# def function's name ():
# todo#      block of code
# ----------------------------------------------------
def test1():
    print("Adham is the best")


test1()


# ------------------------------------------------------
def test2():
    return "Adham is the best"


print(test2())
